已完成交付，写入 `pilot_on_benchmarks.md:1`。
- 包含所需五个章节：`## Objective`、`## Benchmark Matrix`、`## Pilot Result`、`## Residual Risks`、`## Follow-up Checks`
- 文档把结论收敛为：当前方案“适合继续受控 benchmark pilot，但不足以直接做 capability promotion”
- 每条判断都附带了 run pack 内来源，无法直接证明的部分已明确标注为 assumption
- 只修改了交付文件，未触碰 `workflow.*`、`rules.contract.json`、`agent.contract.json` 或 `objects/`

如果你要，我也可以继续帮你起草下一节点 `promote_or_rollback.md` 的判定框架。