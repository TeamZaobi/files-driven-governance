# convergence

## 已收敛

1. 先补 discussion 主路径，不先铺平行资产面。
2. 保留 adversarial example，但不把它放成首轮必读入口。
3. 不把源项目私货带入 files-driven 的正式模板。

## 仍未决

1. `decision_package` 是否需要 schema
2. `process_projection` 是否需要 schema

## 当前裁定

- `result`: `partial_accept`
- `reason`: `主路径已收敛，后续合同化仍需下一轮判断`
- `next_step`: `继续用主路径和条件分支 example 收用户反馈`
