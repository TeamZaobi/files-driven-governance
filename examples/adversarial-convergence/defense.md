# defense

## Q-01

不直接搬源项目的角色名、环境变量、领域对象和编号体系。  
只保留稳定结构：

1. discussion 的最低字段
2. question-level closure
3. decision package 的压缩职责
4. task / decision / archive 的分流判断

## Q-02

第一阶段先补一条主路径：

1. `discussion`
2. `issue_ledger`
3. `decision_package`
4. `task_or_decision`

`adversarial` 和 `process_projection` 只作为条件分支 example，不当第一入口。

## Q-03

这轮先不合同化。  
先让模板和 example 稳定，再决定是否补 schema 和 validator。
