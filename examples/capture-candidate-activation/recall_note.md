# 历史召回

这一页对应 `recall_history`。

## 何时进入

- 新观察和旧事实看起来相似，但还不能确认是否同类问题。
- 需要核对之前是否处理过类似链路、失败或约束。

## 这一页只做什么

- 回收历史，不回收噪声。
- 把相似案例、旧结论和当前观察放到同一个比较面。
- 帮助判断当前材料是补证，还是重复。

## 关键字段

- `recall_id`
- `history_refs`
- `kept_invariants`
- `decision_memory`

## 这页不能做什么

- 不能把历史召回当成最终裁定。
- 不能跳过拆分直接进入试验。
