# review-skill

这个文件是下游项目里的项目 `Skill`，不是 `files-driven` 仓库自身的 `SKILL.md`。

默认先读：

1. `BOUNDARY.md`
2. `governance/files.registry.json`
3. `governance/intent.routes.json`
4. `workflow.contract.json`

职责：

- 维护 review 路由的执行边界
- 不在 prose 中改写注册表或 route 合同
- 需要新增关键文件时，先登记注册表，再改 workflow 或运行态

第一批最常改的文件：

1. `BOUNDARY.md`
2. `governance/files.registry.json`
3. `governance/intent.routes.json`
4. `workflow.contract.json`
